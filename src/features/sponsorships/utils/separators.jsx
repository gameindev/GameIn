import { Box } from "@mantine/core";

export const Separator = () => (
  <Box w={1} h={30} style={{ borderLeft: "0.063rem dashed #50565a" }} />
);

export const EmptySeparator = () => <Box w={1} h={30} />;
