export enum SystemPostCategory {
    WELCOME = 'welcome',                    // Welcome post for new users - visible only to that user in feed, but visible when viewing their profile
    CREATOR_BRAND_COLLISION = 'collision',  // Creator and brand collision when brand sponsors creator
    BROADCAST = 'broadcast',                 // Broadcast post visible to all users
    STATISTICS = 'statistics',              // Statistics updates visible to all users
}

